# Web Framework
fastapi>=0.111.0
uvicorn>=0.29.0
python-multipart>=0.0.9
jinja2>=3.1.4

# HTTP
requests>=2.31.0

# Config + Validation
python-dotenv>=1.0.1
pydantic>=2.0.0

# Vector Store
# NOTE: sentence-transformers removed — embeddings via Ollama (nomic-embed-text)
chromadb>=0.5.0

# Document Parsing (for knowledge_base/ingest.py)
pdfplumber>=0.10.0     # PDF extraction — handles multi-column and tables
python-docx>=1.1.0     # DOCX extraction

# Market Data
yfinance>=0.2.40

# Data
pandas>=2.0.0
numpy>=1.24.0

# ML (Prediction module — Teammates 2+3)
scikit-learn>=1.4.0
